1. create an array with 100 elements with each element printing its number
    creat array variable
2. creat an array that contains numbers from 100 to 1

during break

1. Find max numbers from array
2. Find min numbers from array
